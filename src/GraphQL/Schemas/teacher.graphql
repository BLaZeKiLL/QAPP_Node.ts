# import Status from 'misc.graphql'

input TeacherInput {
  name: String!
  email: String!
  admin: Boolean!
}

type Teacher {
  _id: ID!
  name: String!
  email: String!
  admin: Boolean!
}

type TeacherAuthData {
  id: ID
  email: String
  admin: Boolean
  token: String
  status: Status!
}
