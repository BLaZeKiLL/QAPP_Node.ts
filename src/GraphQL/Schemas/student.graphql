# import Target, TargetInput from 'misc.graphql'
# import Result from 'result.graphql'

input StudentInput {
  name: String!
  email: String!
  rollno: String!
  target: TargetInput!
}

type Student {
  _id: ID!
  name: String!
  email: String!
  deviceID: String
  rollno: String!
  target: Target!
  results: [Result!]
}
